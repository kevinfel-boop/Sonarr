import React from 'react';
import styles from './AppFooter.css';

function AppFooter() {
  return (
    <div className={styles.footer}>
      King Video Master (KVM) — Usage personnel uniquement
    </div>
  );
}

export default AppFooter;
